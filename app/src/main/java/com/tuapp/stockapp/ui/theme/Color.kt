package com.tuapp.stockapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Corporativa y Soberia
val BackgroundCanvas = Color(0xFFF8FAFC) // Slate 50 (Gris limpio de fondo)
val SurfaceCard = Color(0xFFFFFFFF)      // Blanco puro para tarjetas/inputs
val PrimaryNavy = Color(0xFF0F172A)      // Slate 900 (Azul oscuro sobrio)
val SecondarySlate = Color(0xFF334155)   // Slate 700
val BorderStroke = Color(0xFFE2E8F0)     // Slate 200 (Bordes finos)

// Textos
val TextPrimary = Color(0xFF0F172A)
val TextMuted = Color(0xFF64748B)

// Estados / Indicadores
val StatusSuccess = Color(0xFF059669)    // Verde profesional
val StatusDanger = Color(0xFFDC2626)     // Rojo alerta